import React from 'react';
import { InputGroup, Input } from 'reactstrap';

const Example = (props) => {
  return (
    <div>
      <InputGroup>
        <Input placeholder="COMMENT SECTION" />
      </InputGroup>
    </div>
  );
};

export default Example;